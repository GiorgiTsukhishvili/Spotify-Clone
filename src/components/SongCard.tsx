import React from "react";
import { SongInterface } from "../pages/interfaces/songInterface";

const SongCard = ({ song, i }: { song: SongInterface; i: number }) => {
  return <div>SongCard</div>;
};

export default SongCard;
